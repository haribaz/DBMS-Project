<h1 style="text-align: center;">Directors</h1>
<br>
<form method="GET" class="d-flex">
	<input class="form-control" type="text" style="margin-right: 1rem; background-color: var(--color-text-light);" name="title" placeholder="Search Director">
	<button class="btn btn-primary" type="submit">Search</button>
</form>
<br>

<div class="grid-container">
	<% details.forEach((director) => { %>
	<div class="movie-card card">
		<img
			src="<%= /api/images/<%= director.coverImage %>"
			class="card-img-top"
			alt="..."
		/>
		<div class="text-center card-body">
			<h5 class="card-title movie-title"><%= director.name %></h5>
			<div class="follow-grid">
				<form method="post">
					<button type="submit" class="follow-button btn btn-primary">
						Follow
					</button>
				</form>
				<a href="/api/user/director/<%= director.id %>" class="btn btn-primary">Director Page</a>
			</div>
		</div>
	</div>
	<% } %>
</div>

